#include<stdio.h>
int main()
{
    int num=100000;
    for(i=0;i<=num;i++)
    { for(j=2;j<(num/2);j++)
        {
            if((i/2)!=0)
            {
                if((i/j)!=0)
                q[i]=i;
            }
            
        }
    }
    printf("the prime numbers upto 1,00,000 are")
    while(q<100000)
    {
        printf("%d",q);
    }
    return 0;
}
